#pragma once
#include <iostream>
using namespace std;

struct Truck
{
    string driver;
    double petrol;
    string regNo;
    int fullMileage;
    int emptyMileage;

    // Truck(string driver, double petrol,string regNo, int fullMileage, int emptyMileage):driver(driver),
    //                                                                                     petrol(petrol),
    //                                                                                     regNo(regNo),
    //                                                                                     fullMileage(fullMileage),
    //                                                                                     emptyMileage(emptyMileage){}
    
    // TODO: You may need a constructor
    // TODO: You may need to define overloaded relational operators for binary search tree
    // TODO: You may need other helper functions to perform the task in the assignment
};
